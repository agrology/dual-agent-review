---
description: Deprecated alias for /dual-review, which is now autonomous by default.
argument-hint: "[doc-path | PR-URL] [--reviewer <id>] [--attended]"
---

> **Deprecated.** `/dual-review` is autonomous by default as of the BYO-reviewer change, so
> this command exists only so existing muscle memory keeps working. It will be removed in a
> future release.

Follow `dual-review.md` in full — §1 (resolve the argument: split flags, classify, resolve
doc), §2 (arm), §2.5 (route resolution), §3.5 (the unattended loop), §4 (human gate) — with
`$ARGUMENTS` passed through unchanged. There is no behavior here that `dual-review.md` does
not define.
